# Given two numbers, return true if they both have the same sign.

def valuesHaveTheSameSign(x, y):
    # Your code here...
    if (x>=0 and y>=0) or (x<0 and y<0):
       return True
    else:
       return False
